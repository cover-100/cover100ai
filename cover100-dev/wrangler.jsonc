{
  // wrangler is run via npx (or a globally installed wrangler); no local
  // devDependency, so the editor "$schema" line is intentionally omitted.
  "name": "cover100-dev",
  "compatibility_date": "2026-06-11",
  "observability": {
    "enabled": true
  },
  "assets": {
    "directory": "public"
  }
}
